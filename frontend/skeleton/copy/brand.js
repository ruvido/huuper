// Each page carries its own <title> from the layout dict. Overwriting it here
// used to be the only way the brand name reached the tab; it also threw away
// the per-page title that social crawlers and search engines read, so it is
// gone — every page title already ends in the brand name.
window.appCopy = {
  brandName: "Il Branco Realmen",
  shortName: "Il Branco Realmen",
};
